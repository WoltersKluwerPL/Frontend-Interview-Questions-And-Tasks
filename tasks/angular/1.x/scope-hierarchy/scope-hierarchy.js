(function (angular) {
  'use strict';

  /**
   * TODO
   *
   * Write code for two controllers `ParentCtrl` & `ChildCtrl` and create `$scope` hierarchy.
   * It could be any example of an working application.
   */
  angular.module('APP', ['angular-debug-bar']);

  angular.module('APP')
    .controller('ParentCtrl', ParentCtrl)
    .controller('ChildCtrl', ChildCtrl);

  function ParentCtrl () {
    // TODO
  }

  function ChildCtrl () {
    // TODO
  }
})(window.angular);
